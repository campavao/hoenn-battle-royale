# The relay this client connects to (net/relay.ts, POK-219/220). Copy this file to
# .env.local and override for local dev against `cd relay && node server.js`.
VITE_RELAY_URL=wss://hoenn-relay-production.up.railway.app
# VITE_RELAY_URL=ws://localhost:7790
