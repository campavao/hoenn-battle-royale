# catch-buzzer: a ball thrown as the fog arrives (POK-261, and the play-test's own
# freeze -- "we both just disappeared. And then I was frozen here"). Cam's catch was at
# 2:50 of a match whose opening was over by then, so the ring was already up: the
# battle ends into BrMatch_SafariOver, which ends the opening and puts the drop map in
# front of you. What this asks is whether any of that happens at all, or whether the
# battle's own ending leaves the game somewhere with nothing to press.
#
# catch-pages.txt is the same catch without the ring. Everything down to the fourth
# throw is its script, so the two only differ in what is true outside the battle.
#
wait 30
poke u32 gBrMatch+8 0x00000C00
pokebytes gBrMailbox+0x2018 02 00 00 00 00 00 00 00 BD BB C7 FF
wait 400
expect u8 gBrMatch 1
expect u8 gPlayerPartyCount 0
expect u8 gBrMatch+1 0
poke u8 gBrMatch+1 1
poke u16 gBrMatch+12 240
hold DOWN 140
hold LEFT 100
hold RIGHT 70
hold LEFT 70
hold RIGHT 70
hold LEFT 70
wait 40
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
shot try1
tap A
wait 30
hold RIGHT 70
hold LEFT 70
hold RIGHT 70
hold LEFT 70
wait 40
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
shot try2
tap A
wait 30
hold RIGHT 70
hold LEFT 70
hold RIGHT 70
hold LEFT 70
wait 40
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
shot try3
tap A
wait 30
hold RIGHT 70
hold LEFT 70
hold RIGHT 70
hold LEFT 70
wait 40
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap B
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
tap A
wait 30
# The fog is up while the ball is in the air.
poke u8 gBrRing 1
poke u8 gBrRing+1 1
poke u8 gBrRing+4 1
shot thrown
tap A
wait 30
tap A
wait 120
shot interrupted
# From here on nothing is pressed. The box a forced exit prints used to wait to be
# pressed through -- a Safari battle sitting on one line while the fog closes outside,
# which is what the play-test read as a freeze -- so reaching the drop at all, with no
# input at all, is the whole assertion.
wait 700
shot dropmap
expect u8 gBrMatch 2
expect u8 gBrPick 1
say catch buzzer ok
