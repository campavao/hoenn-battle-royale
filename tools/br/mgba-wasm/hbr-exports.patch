diff --git a/include/mgba/internal/gba/renderers/common.h b/include/mgba/internal/gba/renderers/common.h
index ed16917..f0ae805 100644
--- a/include/mgba/internal/gba/renderers/common.h
+++ b/include/mgba/internal/gba/renderers/common.h
@@ -20,7 +20,7 @@ struct GBAVideoRendererSprite {
 	int8_t index;
 };
 
-int GBAVideoRendererCleanOAM(struct GBAObj* oam, struct GBAVideoRendererSprite* sprites, int offsetY);
+int GBAVideoRendererCleanOAM(struct GBAObj* oam, struct GBAVideoRendererSprite* sprites, int offsetY, int visibleWidth, int visibleHeight);
 
 CXX_GUARD_END
 
diff --git a/include/mgba/internal/gba/renderers/video-software.h b/include/mgba/internal/gba/renderers/video-software.h
index ce91877..71adee8 100644
--- a/include/mgba/internal/gba/renderers/video-software.h
+++ b/include/mgba/internal/gba/renderers/video-software.h
@@ -75,10 +75,16 @@ struct WindowControl {
 #define MAX_WINDOW 5
 
 struct Window {
-	uint8_t endX;
+	uint16_t endX;
 	struct WindowControl control;
 };
 
+/* Hoenn Battle Royale (POK-319): the row buffer is wide enough for a band of up to
+ * BR_BAND_MAX pixels either side of the LCD. */
+#define BR_BAND_MAX 128
+#define BR_ROW_MAX (GBA_VIDEO_HORIZONTAL_PIXELS + 2 * BR_BAND_MAX)
+#define BR_ROW_WIDTH(R) (GBA_VIDEO_HORIZONTAL_PIXELS + (R)->brLeft + (R)->brRight)
+
 struct GBAVideoSoftwareRenderer {
 	struct GBAVideoRenderer d;
 
@@ -89,8 +95,8 @@ struct GBAVideoSoftwareRenderer {
 
 	GBARegisterDISPCNT dispcnt;
 
-	uint32_t row[GBA_VIDEO_HORIZONTAL_PIXELS];
-	uint32_t spriteLayer[GBA_VIDEO_HORIZONTAL_PIXELS];
+	uint32_t row[BR_ROW_MAX];
+	uint32_t spriteLayer[BR_ROW_MAX];
 	int32_t spriteCyclesRemaining;
 
 	// BLDCNT
@@ -151,6 +157,14 @@ struct GBAVideoSoftwareRenderer {
 	int end;
 
 	uint8_t lastHighlightAmount;
+
+	/* Hoenn Battle Royale (POK-319): pixels drawn past the LCD on each side, from the
+	 * same register state. The output buffer pointer stays the LCD's top-left; the band
+	 * is at negative offsets from it, so the buffer must have room for it. */
+	int brLeft;
+	int brTop;
+	int brRight;
+	int brBottom;
 };
 
 void GBAVideoSoftwareRendererCreate(struct GBAVideoSoftwareRenderer* renderer);
diff --git a/src/gba/renderers/common.c b/src/gba/renderers/common.c
index e801892..4ad4fb0 100644
--- a/src/gba/renderers/common.c
+++ b/src/gba/renderers/common.c
@@ -7,7 +7,7 @@
 
 #include <mgba/gba/interface.h>
 
-int GBAVideoRendererCleanOAM(struct GBAObj* oam, struct GBAVideoRendererSprite* sprites, int offsetY) {
+int GBAVideoRendererCleanOAM(struct GBAObj* oam, struct GBAVideoRendererSprite* sprites, int offsetY, int visibleWidth, int visibleHeight) {
 	int i;
 	int oamMax = 0;
 	for (i = 0; i < 128; ++i) {
@@ -37,10 +37,13 @@ int GBAVideoRendererCleanOAM(struct GBAObj* oam, struct GBAVideoRendererSprite*
 					cycles += x >> 1;
 				}
 			}
-			if (GBAObjAttributesAGetY(obj.a) >= GBA_VIDEO_VERTICAL_PIXELS && GBAObjAttributesAGetY(obj.a) + height < VIDEO_VERTICAL_TOTAL_PIXELS) {
+			/* Hoenn BR: with a band below the LCD the VBlank rows are drawn, so only cull
+			 * a sprite that sits entirely in rows the band does not reach; a 256-row band
+			 * reaches every row. Likewise the columns past 240 up to the band's edge. */
+			if (visibleHeight < 256 && GBAObjAttributesAGetY(obj.a) >= visibleHeight && GBAObjAttributesAGetY(obj.a) + height < VIDEO_VERTICAL_TOTAL_PIXELS) {
 				continue;
 			}
-			if (GBAObjAttributesBGetX(obj.b) >= GBA_VIDEO_HORIZONTAL_PIXELS && GBAObjAttributesBGetX(obj.b) + width < 512) {
+			if (GBAObjAttributesBGetX(obj.b) >= visibleWidth && GBAObjAttributesBGetX(obj.b) + width < 512) {
 				continue;
 			}
 			int y = GBAObjAttributesAGetY(obj.a) + offsetY;
diff --git a/src/gba/renderers/gl.c b/src/gba/renderers/gl.c
index 5b4ef70..4a54807 100644
--- a/src/gba/renderers/gl.c
+++ b/src/gba/renderers/gl.c
@@ -1432,7 +1432,7 @@ void GBAVideoGLRendererDrawScanline(struct GBAVideoRenderer* renderer, int y) {
 	}
 
 	if (glRenderer->oamDirty) {
-		glRenderer->oamMax = GBAVideoRendererCleanOAM(glRenderer->d.oam->obj, glRenderer->sprites, 0);
+		glRenderer->oamMax = GBAVideoRendererCleanOAM(glRenderer->d.oam->obj, glRenderer->sprites, 0, GBA_VIDEO_HORIZONTAL_PIXELS, GBA_VIDEO_VERTICAL_PIXELS);
 		glRenderer->oamDirty = false;
 	}
 
diff --git a/src/gba/renderers/software-mode0.c b/src/gba/renderers/software-mode0.c
index 4aada17..da68315 100644
--- a/src/gba/renderers/software-mode0.c
+++ b/src/gba/renderers/software-mode0.c
@@ -484,7 +484,7 @@
 	}
 
 void GBAVideoSoftwareRendererDrawBackgroundMode0(struct GBAVideoSoftwareRenderer* renderer, struct GBAVideoSoftwareBackground* background, int y) {
-	int inX = (renderer->start + background->x - background->offsetX) & 0x1FF;
+	int inX = (renderer->start - renderer->brLeft + background->x - background->offsetX) & 0x1FF;
 	int length = renderer->end - renderer->start;
 	if (background->mosaic) {
 		int mosaicV = GBAMosaicControlGetBgV(renderer->mosaic) + 1;
diff --git a/src/gba/renderers/software-obj.c b/src/gba/renderers/software-obj.c
index 186db38..8fcd012 100644
--- a/src/gba/renderers/software-obj.c
+++ b/src/gba/renderers/software-obj.c
@@ -163,7 +163,7 @@ int GBAVideoSoftwareRendererPreprocessSprite(struct GBAVideoSoftwareRenderer* re
 	}
 	int32_t x = (uint32_t) GBAObjAttributesBGetX(sprite->b) << 23;
 	x >>= 23;
-	x += renderer->objOffsetX;
+	x += renderer->objOffsetX + renderer->brLeft;
 	uint16_t* vramBase = &renderer->d.vram[BASE_TILE >> 1];
 	unsigned align = GBAObjAttributesAIs256Color(sprite->a) && !GBARegisterDISPCNTIsObjCharacterMapping(renderer->dispcnt);
 	unsigned charBase = (GBAObjAttributesCGetTile(sprite->c) & ~align) * 0x20;
@@ -221,9 +221,7 @@ int GBAVideoSoftwareRendererPreprocessSprite(struct GBAVideoSoftwareRenderer* re
 		LOAD_16(mat.c, 0, &renderer->d.oam->mat[GBAObjAttributesBGetMatIndex(sprite->b)].c);
 		LOAD_16(mat.d, 0, &renderer->d.oam->mat[GBAObjAttributesBGetMatIndex(sprite->b)].d);
 
-		if (inY < 0) {
-			inY += 256;
-		}
+		inY &= 0xFF;
 		int outX = x >= start ? x : start;
 		int condition = x + totalWidth;
 		int inX = outX - x;
@@ -324,9 +322,7 @@ int GBAVideoSoftwareRendererPreprocessSprite(struct GBAVideoSoftwareRenderer* re
 				condition += mosaicH - (condition % mosaicH);
 			}
 		}
-		if ((int) GBAObjAttributesAGetY(sprite->a) + height - 256 >= 0) {
-			inY += 256;
-		}
+		inY &= 0xFF;
 		if (GBAObjAttributesBIsVFlip(sprite->b)) {
 			inY = height - inY - 1;
 		}
diff --git a/src/gba/renderers/software-private.h b/src/gba/renderers/software-private.h
index 4f7e9f8..6d31d51 100644
--- a/src/gba/renderers/software-private.h
+++ b/src/gba/renderers/software-private.h
@@ -171,8 +171,8 @@ static inline void _compositeNoBlendNoObjwin(struct GBAVideoSoftwareRenderer* re
 	}
 
 #define BACKGROUND_BITMAP_INIT                                                                                        \
-	int32_t x = background->sx + (renderer->start - 1) * background->dx;                                              \
-	int32_t y = background->sy + (renderer->start - 1) * background->dy;                                              \
+	int32_t x = background->sx + (renderer->start - renderer->brLeft - 1) * background->dx;                           \
+	int32_t y = background->sy + (renderer->start - renderer->brLeft - 1) * background->dy;                           \
 	int mosaicH = 0;                                                                                                  \
 	int mosaicWait = 0;                                                                                               \
 	int32_t localX;                                                                                                   \
diff --git a/src/gba/renderers/video-software.c b/src/gba/renderers/video-software.c
index c5bc37b..4090540 100644
--- a/src/gba/renderers/video-software.c
+++ b/src/gba/renderers/video-software.c
@@ -27,6 +27,7 @@ static void GBAVideoSoftwareRendererWriteOAM(struct GBAVideoRenderer* renderer,
 static void GBAVideoSoftwareRendererWritePalette(struct GBAVideoRenderer* renderer, uint32_t address, uint16_t value);
 static uint16_t GBAVideoSoftwareRendererWriteVideoRegister(struct GBAVideoRenderer* renderer, uint32_t address, uint16_t value);
 static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* renderer, int y);
+static void _brDrawLine(struct GBAVideoSoftwareRenderer* renderer, int y, bool band);
 static void GBAVideoSoftwareRendererFinishFrame(struct GBAVideoRenderer* renderer);
 static void GBAVideoSoftwareRendererGetPixels(struct GBAVideoRenderer* renderer, size_t* stride, const void** pixels);
 static void GBAVideoSoftwareRendererPutPixels(struct GBAVideoRenderer* renderer, size_t stride, const void* pixels);
@@ -48,7 +49,7 @@ static void _updatePalettes(struct GBAVideoSoftwareRenderer* renderer);
 static void _updateFlags(struct GBAVideoSoftwareRenderer* renderer, struct GBAVideoSoftwareBackground* bg);
 
 static void _breakWindow(struct GBAVideoSoftwareRenderer* softwareRenderer, struct WindowN* win);
-static void _breakWindowInner(struct GBAVideoSoftwareRenderer* softwareRenderer, struct WindowN* win);
+static void _breakWindowInner(struct GBAVideoSoftwareRenderer* softwareRenderer, int hStart, int hEnd, struct WindowControl control);
 
 void GBAVideoSoftwareRendererCreate(struct GBAVideoSoftwareRenderer* renderer) {
 	memset(renderer, 0, sizeof(*renderer));
@@ -444,40 +445,46 @@ static void GBAVideoSoftwareRendererWritePalette(struct GBAVideoRenderer* render
 }
 
 static void _breakWindow(struct GBAVideoSoftwareRenderer* softwareRenderer, struct WindowN* win) {
+	/* Hoenn BR: window edges are LCD coordinates; the row buffer starts brLeft before.
+	 * An edge on the LCD's own border reaches across the band: the picture past the
+	 * LCD is what the LCD's edge pixel would see, continued. (Emerald's overworld is a
+	 * full-screen WIN0 with every layer inside and only BG0 outside, so without this
+	 * the band would be the backdrop.) */
+	int left = softwareRenderer->brLeft;
+	int width = BR_ROW_WIDTH(softwareRenderer);
+	int start = win->h.start == 0 ? 0 : win->h.start + left;
+	int end = win->h.end >= GBA_VIDEO_HORIZONTAL_PIXELS ? width : win->h.end + left;
 	if (win->h.end > GBA_VIDEO_HORIZONTAL_PIXELS || win->h.end < win->h.start) {
-		struct WindowN splits[2] = { *win, *win };
-		splits[0].h.start = 0;
-		splits[1].h.end = GBA_VIDEO_HORIZONTAL_PIXELS;
-		_breakWindowInner(softwareRenderer, &splits[0]);
-		_breakWindowInner(softwareRenderer, &splits[1]);
+		_breakWindowInner(softwareRenderer, 0, win->h.end + left, win->control);
+		_breakWindowInner(softwareRenderer, start, width, win->control);
 	} else {
-		_breakWindowInner(softwareRenderer, win);
+		_breakWindowInner(softwareRenderer, start, end, win->control);
 	}
 }
 
-static void _breakWindowInner(struct GBAVideoSoftwareRenderer* softwareRenderer, struct WindowN* win) {
+static void _breakWindowInner(struct GBAVideoSoftwareRenderer* softwareRenderer, int hStart, int hEnd, struct WindowControl control) {
 	int activeWindow;
 	int startX = 0;
-	if (win->h.end > 0) {
+	if (hEnd > 0) {
 		for (activeWindow = 0; activeWindow < softwareRenderer->nWindows; ++activeWindow) {
-			if (win->h.start < softwareRenderer->windows[activeWindow].endX) {
+			if (hStart < softwareRenderer->windows[activeWindow].endX) {
 				// Insert a window before the end of the active window
 				struct Window oldWindow = softwareRenderer->windows[activeWindow];
-				if (win->h.start > startX) {
+				if (hStart > startX) {
 					// And after the start of the active window
 					int nextWindow = softwareRenderer->nWindows;
 					++softwareRenderer->nWindows;
 					for (; nextWindow > activeWindow; --nextWindow) {
 						softwareRenderer->windows[nextWindow] = softwareRenderer->windows[nextWindow - 1];
 					}
-					softwareRenderer->windows[activeWindow].endX = win->h.start;
+					softwareRenderer->windows[activeWindow].endX = hStart;
 					++activeWindow;
 				}
-				softwareRenderer->windows[activeWindow].control = win->control;
-				softwareRenderer->windows[activeWindow].endX = win->h.end;
-				if (win->h.end >= oldWindow.endX) {
+				softwareRenderer->windows[activeWindow].control = control;
+				softwareRenderer->windows[activeWindow].endX = hEnd;
+				if (hEnd >= oldWindow.endX) {
 					// Trim off extra windows we've overwritten
-					for (++activeWindow; softwareRenderer->nWindows > activeWindow + 1 && win->h.end >= softwareRenderer->windows[activeWindow].endX; ++activeWindow) {
+					for (++activeWindow; softwareRenderer->nWindows > activeWindow + 1 && hEnd >= softwareRenderer->windows[activeWindow].endX; ++activeWindow) {
 						if (VIDEO_CHECKS && activeWindow >= MAX_WINDOW) {
 							mLOG(GBA_VIDEO, FATAL, "Out of bounds window write will occur");
 							return;
@@ -610,10 +617,19 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 
 	CLEAN_SCANLINE(softwareRenderer, y);
 
-	mColor* row = &softwareRenderer->outputBuffer[softwareRenderer->outputBufferStride * y];
+	_brDrawLine(softwareRenderer, y, false);
+}
+
+/* Hoenn BR (POK-319): one row of the wide picture -- the LCD's 240 pixels plus the band
+ * either side -- into the output buffer at row `y`, which may be negative or past 159
+ * for a band row. A band row is drawn from the frame's final register state, never
+ * caches, never advances the affine or enable state, and only shows text layers. */
+static void _brDrawLine(struct GBAVideoSoftwareRenderer* softwareRenderer, int y, bool band) {
+	int width = BR_ROW_WIDTH(softwareRenderer);
+	mColor* row = &softwareRenderer->outputBuffer[softwareRenderer->outputBufferStride * y - softwareRenderer->brLeft];
 	if (GBARegisterDISPCNTIsForcedBlank(softwareRenderer->dispcnt)) {
 		int x;
-		for (x = 0; x < GBA_VIDEO_HORIZONTAL_PIXELS; ++x) {
+		for (x = 0; x < width; ++x) {
 			row[x] = M_COLOR_WHITE;
 		}
 		return;
@@ -648,15 +664,27 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 					break;
 				case 1:
 				case 2:
+					if (band) {
+						break;
+					}
 					GBAVideoSoftwareRendererDrawBackgroundMode2(softwareRenderer, &softwareRenderer->bg[2], y);
 					break;
 				case 3:
+					if (band) {
+						break;
+					}
 					GBAVideoSoftwareRendererDrawBackgroundMode3(softwareRenderer, &softwareRenderer->bg[2], y);
 					break;
 				case 4:
+					if (band) {
+						break;
+					}
 					GBAVideoSoftwareRendererDrawBackgroundMode4(softwareRenderer, &softwareRenderer->bg[2], y);
 					break;
 				case 5:
+					if (band) {
+						break;
+					}
 					GBAVideoSoftwareRendererDrawBackgroundMode5(softwareRenderer, &softwareRenderer->bg[2], y);
 					break;
 				}
@@ -667,6 +695,9 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 					GBAVideoSoftwareRendererDrawBackgroundMode0(softwareRenderer, &softwareRenderer->bg[3], y);
 					break;
 				case 2:
+					if (band) {
+						break;
+					}
 					GBAVideoSoftwareRendererDrawBackgroundMode2(softwareRenderer, &softwareRenderer->bg[3], y);
 					break;
 				}
@@ -676,6 +707,11 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 
 	GBAVideoSoftwareRendererPostprocessBuffer(softwareRenderer);
 
+	if (band) {
+		memcpy(row, softwareRenderer->row, width * sizeof(*row));
+		return;
+	}
+
 	if (GBARegisterDISPCNTGetMode(softwareRenderer->dispcnt) != 0) {
 		if (softwareRenderer->bg[2].enabled == ENABLED_MAX) {
 			softwareRenderer->bg[2].sx += softwareRenderer->bg[2].dmx;
@@ -706,7 +742,7 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 
 	int x;
 	if (softwareRenderer->stereo) {
-		for (x = 0; x < GBA_VIDEO_HORIZONTAL_PIXELS; x += 4) {
+		for (x = 0; x < width; x += 4) {
 			row[x] = softwareRenderer->row[x] & (M_COLOR_RED | M_COLOR_BLUE);
 			row[x] |= softwareRenderer->row[x + 1] & M_COLOR_GREEN;
 			row[x + 1] = softwareRenderer->row[x + 1] & (M_COLOR_RED | M_COLOR_BLUE);
@@ -719,21 +755,65 @@ static void GBAVideoSoftwareRendererDrawScanline(struct GBAVideoRenderer* render
 		}
 	} else {
 #ifdef COLOR_16_BIT
-		for (x = 0; x < GBA_VIDEO_HORIZONTAL_PIXELS; x += 4) {
+		for (x = 0; x < width; x += 4) {
 			row[x] = softwareRenderer->row[x];
 			row[x + 1] = softwareRenderer->row[x + 1];
 			row[x + 2] = softwareRenderer->row[x + 2];
 			row[x + 3] = softwareRenderer->row[x + 3];
 		}
 #else
-		memcpy(row, softwareRenderer->row, GBA_VIDEO_HORIZONTAL_PIXELS * sizeof(*row));
+		memcpy(row, softwareRenderer->row, width * sizeof(*row));
 #endif
 	}
 }
 
+/* Hoenn BR (POK-319): is window `win` open on a row the LCD never has? A band row
+ * continues the LCD's nearest edge row, so it takes that row's answer: the hardware
+ * compares VCOUNT with the start and end lines, and that compare is made for row 0
+ * above the LCD and row 159 below it. */
+static bool _brWindowOn(const struct WindowN* win, int y) {
+	int start = win->v.start + win->offsetY;
+	int end = win->v.end + win->offsetY;
+	if (y < 0) {
+		y = 0;
+	} else if (y >= GBA_VIDEO_VERTICAL_PIXELS) {
+		y = GBA_VIDEO_VERTICAL_PIXELS - 1;
+	}
+	if (start <= end) {
+		return y >= start && y < end;
+	}
+	return y >= start || y < end;
+}
+
+/* Hoenn BR (POK-319): the rows above and below the LCD, drawn at VBlank from the state
+ * the frame ended with. Emerald writes its scroll registers in the VBlank handler, which
+ * runs after this, so the band matches the frame it borders. */
+static void _brDrawBand(struct GBAVideoSoftwareRenderer* softwareRenderer) {
+	int y;
+	bool on0 = softwareRenderer->winN[0].on;
+	bool on1 = softwareRenderer->winN[1].on;
+	for (y = -softwareRenderer->brTop; y < GBA_VIDEO_VERTICAL_PIXELS + softwareRenderer->brBottom; ++y) {
+		if (y == 0) {
+			y = GBA_VIDEO_VERTICAL_PIXELS;
+			if (y >= GBA_VIDEO_VERTICAL_PIXELS + softwareRenderer->brBottom) {
+				break;
+			}
+		}
+		softwareRenderer->winN[0].on = _brWindowOn(&softwareRenderer->winN[0], y);
+		softwareRenderer->winN[1].on = _brWindowOn(&softwareRenderer->winN[1], y);
+		_brDrawLine(softwareRenderer, y, true);
+	}
+	softwareRenderer->winN[0].on = on0;
+	softwareRenderer->winN[1].on = on1;
+}
+
 static void GBAVideoSoftwareRendererFinishFrame(struct GBAVideoRenderer* renderer) {
 	struct GBAVideoSoftwareRenderer* softwareRenderer = (struct GBAVideoSoftwareRenderer*) renderer;
 
+	if (softwareRenderer->outputBuffer && (softwareRenderer->brTop || softwareRenderer->brBottom || softwareRenderer->brLeft || softwareRenderer->brRight)) {
+		_brDrawBand(softwareRenderer);
+	}
+
 	softwareRenderer->nextY = 0;
 	if (softwareRenderer->temporaryBuffer) {
 		mappedMemoryFree(softwareRenderer->temporaryBuffer, GBA_VIDEO_HORIZONTAL_PIXELS * GBA_VIDEO_VERTICAL_PIXELS * 4);
@@ -893,14 +973,15 @@ void GBAVideoSoftwareRendererStepWindow(struct GBAVideoSoftwareRenderer* softwar
 
 void GBAVideoSoftwareRendererPreprocessBuffer(struct GBAVideoSoftwareRenderer* softwareRenderer) {
 	int x;
-	for (x = 0; x < GBA_VIDEO_HORIZONTAL_PIXELS; x += 4) {
+	int width = BR_ROW_WIDTH(softwareRenderer);
+	for (x = 0; x < width; x += 4) {
 		softwareRenderer->spriteLayer[x] = FLAG_UNWRITTEN;
 		softwareRenderer->spriteLayer[x + 1] = FLAG_UNWRITTEN;
 		softwareRenderer->spriteLayer[x + 2] = FLAG_UNWRITTEN;
 		softwareRenderer->spriteLayer[x + 3] = FLAG_UNWRITTEN;
 	}
 
-	softwareRenderer->windows[0].endX = GBA_VIDEO_HORIZONTAL_PIXELS;
+	softwareRenderer->windows[0].endX = width;
 	softwareRenderer->nWindows = 1;
 	if (GBARegisterDISPCNTIsWin0Enable(softwareRenderer->dispcnt) || GBARegisterDISPCNTIsWin1Enable(softwareRenderer->dispcnt) || GBARegisterDISPCNTIsObjwinEnable(softwareRenderer->dispcnt)) {
 		softwareRenderer->windows[0].control = softwareRenderer->winout;
@@ -1021,7 +1102,7 @@ int GBAVideoSoftwareRendererPreprocessSpriteLayer(struct GBAVideoSoftwareRendere
 	int spriteLayers = 0;
 	if (GBARegisterDISPCNTIsObjEnable(renderer->dispcnt) && !renderer->d.disableOBJ) {
 		if (renderer->oamDirty) {
-			renderer->oamMax = GBAVideoRendererCleanOAM(renderer->d.oam->obj, renderer->sprites, renderer->objOffsetY);
+			renderer->oamMax = GBAVideoRendererCleanOAM(renderer->d.oam->obj, renderer->sprites, renderer->objOffsetY, GBA_VIDEO_HORIZONTAL_PIXELS + renderer->brRight, GBA_VIDEO_VERTICAL_PIXELS + renderer->brTop + renderer->brBottom);
 			renderer->oamDirty = false;
 		}
 		int mosaicV = GBAMosaicControlGetObjV(renderer->mosaic) + 1;
@@ -1037,7 +1118,7 @@ int GBAVideoSoftwareRendererPreprocessSpriteLayer(struct GBAVideoSoftwareRendere
 			if (renderer->spriteCyclesRemaining <= 0) {
 				break;
 			}
-			if (y < sprite->y || y >= sprite->endY) {
+			if (((y - sprite->y) & 0xFF) >= sprite->endY - sprite->y) {
 				continue;
 			}
 			if (GBAObjAttributesAIsMosaic(sprite->obj.a) && mosaicV > 1) {
diff --git a/src/platform/wasm/CMakeLists.txt b/src/platform/wasm/CMakeLists.txt
index 751e8b1..59daede 100644
--- a/src/platform/wasm/CMakeLists.txt
+++ b/src/platform/wasm/CMakeLists.txt
@@ -2,7 +2,7 @@ set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} \
 	-O3 \
 	-s USE_SDL=2 \
 	-s INITIAL_MEMORY=256mb \
-	-s EXPORTED_RUNTIME_METHODS=[\"FS\",\"cwrap\",\"addFunction\",\"removeFunction\"] \
+	-s EXPORTED_RUNTIME_METHODS=[\"FS\",\"cwrap\",\"addFunction\",\"removeFunction\",\"HEAPU8\",\"HEAPU16\",\"HEAPU32\"] \
 	-s ALLOW_TABLE_GROWTH=1 \
 	-s ENVIRONMENT='web,worker' \
 	-s PTHREAD_POOL_SIZE=5 \
diff --git a/src/platform/wasm/main.c b/src/platform/wasm/main.c
index 14d9a8a..9b78c21 100644
--- a/src/platform/wasm/main.c
+++ b/src/platform/wasm/main.c
@@ -12,6 +12,10 @@
 #include <mgba/core/version.h>
 #include <mgba/gba/interface.h>
 #include <mgba/internal/gba/input.h>
+#include <mgba/core/interface.h>
+#include <mgba/internal/gba/gba.h>
+#include <mgba/internal/gba/memory.h>
+#include <mgba/internal/gba/renderers/video-software.h>
 
 #include "platform/sdl/sdl-audio.h"
 #include "platform/sdl/sdl-events.h"
@@ -146,6 +150,161 @@ EMSCRIPTEN_KEEPALIVE float getVolume() {
 		return 0.0;
 }
 
+// Hoenn Battle Royale (POK-319): the picture past the LCD. The page asks for a band of
+// pixels on each side before loadGame; the texture and canvas grow to fit, the core's
+// video buffer pointer is the LCD's top-left inside that texture, and the software
+// renderer fills the band at VBlank from the same registers. Multiples of 8, at most
+// BR_BAND_MAX a side (the renderer's row buffer).
+static int brBandLeft = 0;
+static int brBandTop = 0;
+static int brBandRight = 0;
+static int brBandBottom = 0;
+// Module.brHeadless at start-up: this instance draws to nothing (a page's second core).
+// SDL's emscripten video names its canvas "#canvas" by selector, whichever element
+// Module.canvas is, so a second instance with a window would resize the first's canvas.
+static bool brHeadless = false;
+static mColor* brHeadlessBuffer = NULL;
+static size_t brHeadlessBufferSize = 0;
+// Once the page has called brPresent() from the frame-ended callback, the run loop
+// stops presenting on its own: the page presents each frame while the core thread is
+// paused in that callback, so what it draws around the picture is the same frame as
+// the picture. Left to the run loop, the upload lands on the browser's animation tick
+// while the core runs on, and the picture can be a frame ahead of the page's drawing
+// (a 4 px step between the map copy and the band while walking, 2026-09-18).
+static bool brPagePresents = false;
+
+static int _brClampBand(int v) {
+	if (v < 0) {
+		v = 0;
+	}
+	if (v > BR_BAND_MAX) {
+		v = BR_BAND_MAX;
+	}
+	return v & ~7;
+}
+
+EMSCRIPTEN_KEEPALIVE void brSetViewport(int left, int top, int right, int bottom) {
+	brBandLeft = _brClampBand(left);
+	brBandTop = _brClampBand(top);
+	brBandRight = _brClampBand(right);
+	brBandBottom = _brClampBand(bottom);
+	// Size the window now, before a game is loaded: the first loadGame after the window
+	// was made at the LCD's size otherwise resizes it under a fresh renderer, and the
+	// canvas came up blank that way.
+	if (renderer && renderer->window && !renderer->core && !brHeadless) {
+		int w = GBA_VIDEO_HORIZONTAL_PIXELS + brBandLeft + brBandRight;
+		int h = GBA_VIDEO_VERTICAL_PIXELS + brBandTop + brBandBottom;
+		SDL_SetWindowSize(renderer->window, w, h);
+		EM_ASM(
+		    {
+			    Module.canvas.width = $0;
+			    Module.canvas.height = $1;
+		    },
+		    w, h);
+	}
+}
+
+// The picture's pixels in the wasm heap (ABGR8888, the texture's top-left, `stride`
+// pixels a row), for the page's own tests of the band; 0 with no game loaded.
+EMSCRIPTEN_KEEPALIVE uint32_t brPicturePtr() {
+	if (!renderer->core || !renderer->outputBuffer) {
+		return 0;
+	}
+	return (uint32_t) (uintptr_t) renderer->outputBuffer;
+}
+
+EMSCRIPTEN_KEEPALIVE int brPictureStride() {
+	return GBA_VIDEO_HORIZONTAL_PIXELS + brBandLeft + brBandRight;
+}
+
+static void _brPictureSize(unsigned* w, unsigned* h) {
+	renderer->core->baseVideoSize(renderer->core, w, h);
+	*w += brBandLeft + brBandRight;
+	*h += brBandTop + brBandBottom;
+}
+
+// Lock the texture (or size the headless buffer) and hand the core the LCD's top-left
+// inside it.
+static void _brBindVideoBuffer(void) {
+	int stride;
+	if (brHeadless) {
+		unsigned w, h;
+		_brPictureSize(&w, &h);
+		if (brHeadlessBufferSize < (size_t) w * h) {
+			free(brHeadlessBuffer);
+			brHeadlessBufferSize = (size_t) w * h;
+			brHeadlessBuffer = calloc(brHeadlessBufferSize, BYTES_PER_PIXEL);
+		}
+		renderer->outputBuffer = brHeadlessBuffer;
+		stride = w;
+	} else {
+		SDL_LockTexture(renderer->sdlTex, 0, (void**) &renderer->outputBuffer, &stride);
+		stride /= BYTES_PER_PIXEL;
+	}
+	renderer->core->setVideoBuffer(renderer->core, renderer->outputBuffer + brBandTop * stride + brBandLeft, stride);
+}
+
+void drawFPS(unsigned x, unsigned y);
+
+// The texture to the canvas, then the buffer handed back to the core.
+static void _brPresentFrame(void) {
+	unsigned w, h;
+	_brPictureSize(&w, &h);
+
+	SDL_Rect rect = { .x = 0, .y = 0, .w = w, .h = h };
+
+	SDL_UnlockTexture(renderer->sdlTex);
+	SDL_RenderCopy(renderer->sdlRenderer, renderer->sdlTex, &rect, &rect);
+	if (renderer->showFpsCounter)
+		drawFPS(w - 35, h - LINE_HEIGHT);
+	SDL_RenderPresent(renderer->sdlRenderer);
+	_brBindVideoBuffer();
+}
+
+// Hoenn BR: present the frame from the page's frame-ended callback (main thread, the
+// core thread paused inside that callback), so the page's own drawing and the picture
+// are the same frame. Takes the presenting over from the run loop for good.
+EMSCRIPTEN_KEEPALIVE void brPresent(void) {
+	if (!renderer->core || !renderer->thread || brHeadless || !renderer->sdlTex) {
+		return;
+	}
+	brPagePresents = true;
+	_brPresentFrame();
+}
+
+// Tell the software renderer about the band, once the core has installed it.
+static void _brApplyViewport(void) {
+	if (!renderer->core || renderer->core->platform(renderer->core) != mPLATFORM_GBA) {
+		return;
+	}
+	struct GBA* gba = (struct GBA*) renderer->core->board;
+	struct GBAVideoRenderer* vr = gba->video.renderer;
+	if (!vr || !vr->rendererId || vr->rendererId(vr) != 0x6E727773) {
+		return;
+	}
+	struct GBAVideoSoftwareRenderer* sr = (struct GBAVideoSoftwareRenderer*) vr;
+	sr->brLeft = brBandLeft;
+	sr->brTop = brBandTop;
+	sr->brRight = brBandRight;
+	sr->brBottom = brBandBottom;
+}
+
+// Hoenn Battle Royale: byte offsets into the wasm heap of the GBA's work RAM, so the
+// page can read/write EWRAM/IWRAM through Module.HEAPU8 with no copy. 0 when no GBA core.
+EMSCRIPTEN_KEEPALIVE uint32_t brWramPtr() {
+	if (!renderer->core || renderer->core->platform(renderer->core) != mPLATFORM_GBA)
+		return 0;
+	struct GBA* gba = (struct GBA*) renderer->core->board;
+	return (uint32_t) (uintptr_t) gba->memory.wram;
+}
+
+EMSCRIPTEN_KEEPALIVE uint32_t brIwramPtr() {
+	if (!renderer->core || renderer->core->platform(renderer->core) != mPLATFORM_GBA)
+		return 0;
+	struct GBA* gba = (struct GBA*) renderer->core->board;
+	return (uint32_t) (uintptr_t) gba->memory.iwram;
+}
+
 EMSCRIPTEN_KEEPALIVE int getMainLoopTimingMode() {
 	int mode = -1;
 	int value = -1;
@@ -427,28 +586,30 @@ EMSCRIPTEN_KEEPALIVE bool loadGame(const char* name, const char* savePathOverrid
 	mSDLInitBindingsGBA(&renderer->core->inputMap);
 
 	unsigned w, h;
-	renderer->core->baseVideoSize(renderer->core, &w, &h);
-	if (renderer->sdlTex) {
-		SDL_DestroyTexture(renderer->sdlTex);
+	_brPictureSize(&w, &h);
+	if (!brHeadless) {
+		if (renderer->sdlTex) {
+			SDL_DestroyTexture(renderer->sdlTex);
+		}
+		renderer->sdlTex =
+		    SDL_CreateTexture(renderer->sdlRenderer, SDL_PIXELFORMAT_ABGR8888, SDL_TEXTUREACCESS_STREAMING, w, h);
 	}
-	renderer->sdlTex =
-	    SDL_CreateTexture(renderer->sdlRenderer, SDL_PIXELFORMAT_ABGR8888, SDL_TEXTUREACCESS_STREAMING, w, h);
 
-	int stride;
-	SDL_LockTexture(renderer->sdlTex, 0, (void**) &renderer->outputBuffer, &stride);
-	renderer->core->setVideoBuffer(renderer->core, renderer->outputBuffer, stride / BYTES_PER_PIXEL);
+	_brBindVideoBuffer();
 	renderer->core->setAudioBufferSize(renderer->core, renderer->audio.samples);
 
 	renderer->core->reset(renderer->core);
-
-	renderer->core->currentVideoSize(renderer->core, &w, &h);
-	SDL_SetWindowSize(renderer->window, w, h);
-	EM_ASM(
-	    {
-		    Module.canvas.width = $0;
-		    Module.canvas.height = $1;
-	    },
-	    w, h);
+	_brApplyViewport();
+
+	if (!brHeadless) {
+		SDL_SetWindowSize(renderer->window, w, h);
+		EM_ASM(
+		    {
+			    Module.canvas.width = $0;
+			    Module.canvas.height = $1;
+		    },
+		    w, h);
+	}
 
 	emscripten_resume_main_loop();
 	return true;
@@ -744,19 +905,11 @@ void runLoop() {
 				};
 			}
 			if (mCoreSyncWaitFrameStart(&renderer->thread->impl->sync)) {
-				unsigned w, h;
-				renderer->core->currentVideoSize(renderer->core, &w, &h);
-
-				SDL_Rect rect = { .x = 0, .y = 0, .w = w, .h = h };
-
-				SDL_UnlockTexture(renderer->sdlTex);
-				SDL_RenderCopy(renderer->sdlRenderer, renderer->sdlTex, &rect, &rect);
-				if (renderer->showFpsCounter)
-					drawFPS(w - 35, h - LINE_HEIGHT);
-				SDL_RenderPresent(renderer->sdlRenderer);
-				int stride;
-				SDL_LockTexture(renderer->sdlTex, 0, (void**) &renderer->outputBuffer, &stride);
-				renderer->core->setVideoBuffer(renderer->core, renderer->outputBuffer, stride / BYTES_PER_PIXEL);
+				if (brHeadless) {
+					_brBindVideoBuffer();
+				} else if (!brPagePresents) {
+					_brPresentFrame();
+				}
 			}
 			mCoreSyncWaitFrameEnd(&renderer->thread->impl->sync);
 		}
@@ -860,11 +1013,16 @@ int main() {
 
 	mLogSetDefaultLogger(&logCtx);
 
+	// Hoenn BR: a page's second instance draws to nothing (see brHeadless above).
+	brHeadless = EM_ASM_INT({ return Module.brHeadless ? 1 : 0; }) != 0;
+	// (Video init only registers display modes; the canvas is touched by a window.)
 	SDL_Init(SDL_INIT_VIDEO);
-	renderer->window = SDL_CreateWindow(NULL, SDL_WINDOWPOS_UNDEFINED, SDL_WINDOWPOS_UNDEFINED,
-	                                    GBA_VIDEO_HORIZONTAL_PIXELS, GBA_VIDEO_VERTICAL_PIXELS, SDL_WINDOW_OPENGL);
-	renderer->sdlRenderer =
-	    SDL_CreateRenderer(renderer->window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC);
+	if (!brHeadless) {
+		renderer->window = SDL_CreateWindow(NULL, SDL_WINDOWPOS_UNDEFINED, SDL_WINDOWPOS_UNDEFINED,
+		                                    GBA_VIDEO_HORIZONTAL_PIXELS, GBA_VIDEO_VERTICAL_PIXELS, SDL_WINDOW_OPENGL);
+		renderer->sdlRenderer =
+		    SDL_CreateRenderer(renderer->window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC);
+	}
 
 	// exclude specific key events
 	SDL_SetEventFilter(excludeKeys, NULL);
diff --git a/src/platform/wasm/mgba.d.ts b/src/platform/wasm/mgba.d.ts
index edad1b1..d279d1b 100644
--- a/src/platform/wasm/mgba.d.ts
+++ b/src/platform/wasm/mgba.d.ts
@@ -296,6 +296,32 @@ declare namespace mGBA {
      * @returns Current volume multiplier.
      */
     getVolume(): number;
+    /** Hoenn BR (POK-319): render a band of pixels past the LCD on each side, from the
+     *  same registers. Call before loadGame; multiples of 8, at most 128 a side. The
+     *  canvas becomes (240 + left + right) x (160 + top + bottom) with the LCD at
+     *  (left, top). */
+    _brSetViewport(left: number, top: number, right: number, bottom: number): void;
+    /** Hoenn BR: pass `brHeadless: true` in the module options to make an instance that
+     *  draws to nothing -- no SDL window, no canvas. A page's second core must, because
+     *  SDL's emscripten video names its canvas "#canvas" by selector, whichever element
+     *  Module.canvas is, and a second window would resize the first's canvas. */
+    brHeadless?: boolean;
+    /** Hoenn BR: present the frame to the canvas now. Call it from the video-frame-ended
+     *  callback, where the core thread is paused, after drawing whatever must match the
+     *  picture; from then on the run loop leaves presenting to the page. */
+    _brPresent(): void;
+    /** Hoenn BR (POK-319): wasm heap offset of the picture's ABGR8888 pixels (the texture's
+     *  top-left; the LCD is at (left, top) inside it), 0 with no game loaded. */
+    _brPicturePtr(): number;
+    /** Hoenn BR (POK-319): pixels per row of that picture. */
+    _brPictureStride(): number;
+    /** Hoenn BR: wasm heap offset of GBA EWRAM (256 KiB), 0 when no GBA core is loaded. */
+    _brWramPtr(): number;
+    /** Hoenn BR: wasm heap offset of GBA IWRAM (32 KiB), 0 when no GBA core is loaded. */
+    _brIwramPtr(): number;
+    HEAPU8: Uint8Array;
+    HEAPU16: Uint16Array;
+    HEAPU32: Uint32Array;
 
     /**
      * Lists ROM file names under the game directory.
